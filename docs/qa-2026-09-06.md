# Проверка после ручной приёмки 06.09.2026

## Исправления

- Контейнер экранов растёт по содержимому. Длинный список сделок больше не выходит за его нижний отступ; сохранён запас перед навигацией.
- Карточки допускают перенос непрерывного текста. Имя загруженного файла в уведомлении ограничено шириной и сокращается многоточием; полный текст остаётся в атрибуте title. Иконка не сжимается.
- У принятого или отклонённого материала кнопка «Результат проверки» открывает просмотр сохранённого решения. Кнопок повторного принятия/отклонения нет.
- API атомарно принимает решение только для PENDING. Повторный или конкурирующий запрос получает 409 / FILE_ALREADY_REVIEWED; транзакция не меняет время сделки, доверие, согласования или журнал. Исправленный документ загружается отдельным файлом.

## Реальная сделка МК-20260906-9C306736-V1

- Статус COMPLETED, два участника, два согласования, две подписи одного снимка. Завершена 06.09.2026 в 10:31:05 по Москве.
- PDF: 34 672 байта; SHA-256 `523c52bc78c2110c16932f59ccffafa4672cead759b37e8f3cd24279cbd4714d`.
- ZIP: 1 152 678 байт; SHA-256 `5a204f34498fdf310e905e08e3ddd76e8265ee8be4926f610667a0037ef2bf1c`.
- Приложенные пользователем файлы побайтно совпадают с записями сервера. Все восемь файлов manifest прошли проверку размера и SHA-256. PDF внутри ZIP идентичен отдельному договору.
- Независимо пересчитан хеш frozenSnapshot; обе подписи указывают на него. У всех восьми объектов сделки в S3 (шесть загрузок и два итоговых файла) фактические размеры и SHA-256 совпали с базой.
- В архиве четыре общих вложения (5, 2, 5 и 1 страница); два из них одинаковы по содержимому, поскольку файл загружен в разные требования. Оба OWNER_ONLY документа отсутствуют в архиве.
- Обе страницы договора отрендерены и просмотрены: кириллица, реквизиты, подписи, SHA-256, QR и колонтитулы читаются, пересечений нет. Сам QR декодирован и совпал с URL публичной проверки. API возвращает VALID и тот же хеш PDF.
- История содержит пять решений ACCEPTED по одному документу — подтверждение исправляемой ошибки повторной проверки. Старые записи истории и выданные файлы не изменяются.

## Почему не было вопросов

В базе сохранён успешный ответ Yandex, questions=[], questionHistory=[], clarificationAnswers=null. Это не потеря экрана или сбой запроса: модель сочла анкету достаточной. Введены ремонт ванной под ключ, стоимость 30 000, даты 08–18 сентября, материалы включены, место «Квартира собственника».

Проверка содержания показала незакрытый вопрос полноты: нет конкретного места работ и схемы оплаты, условия приёмки сформулированы моделью без отдельного ответа пользователя. Ограничение отражено в CLIENT_HANDOFF_REMAINING.md. Эта выкладка исправляет интерфейс и однократность ручной проверки, но не меняет анкету/промпты и не переписывает подписанный договор.

## Ручная проверка в MAX

Изолированная проверка PostgreSQL: 19 тестов прошли, включая гонку двух решений и сохранение данных при повторном отклонении. Проверка FilesService: 10 тестов прошли. Проверка вёрстки: отдельный браузерный сценарий прошёл для 1 и 12 сделок на 320×320, 390×844 и 1440×900; проверены также нижние границы профиля и списка документов. Минимальный отступ — 20 px. Снимки просмотрены, стиль сверен с действующим Jeton-референсом: сохранены светлые карточки, оранжевый акцент и скругления.

1. Полностью закрыть мини-приложение и открыть снова. На вкладке «Сделки» прокрутить длинный список до кнопки «Создать ещё сделку» — между ней и нижней навигацией должен быть отступ.
2. В незавершённой сделке загрузить файл с длинным именем. Зелёное уведомление остаётся внутри карточки; имя сокращается, иконка видна.
3. В админке открыть «Проверка файлов»: у обработанных файлов доступен «Результат проверки», только просмотр и скачивание. У ожидающего файла принять решение один раз, затем проверить просмотр результата.
4. Открыть последнюю завершённую сделку и страницу QR: статус завершения и проверка целостности сохраняются.
